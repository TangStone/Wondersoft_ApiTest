- epic: 系统管理   # 一级模块
  feature: 运维管理-Syslog规则配置  # 二级模块
  story: 获取所有可用于syslog的元数据字段信息接口    # 接口名称
  # 接口基本信息
  case_info:
    # 基础URL:https://192.168.148.174:31000
    base_url: ${config(host)}
    # 请求信息
    request:
      # 请求类型
      method: GET
      # 请求地址
      address: /api/auth/v1/metadata/getAssetTreeByMetadata
      # 请求头
      headers:
        Content-Type: application/x-www-form-urlencoded
        token: ${extract(token)}
        Referer: ${config(host)}/sub-app-unity/syslogManage
  # 用例信息，可包含多个用例，用例id需唯一
  case_data:
    # 用例id
    syslogrule_getappcode_01:
      # 用例名称
      name: 获取所有可用于syslog的元数据字段信息
      # 用例描述
      description: 获取所有可用于syslog的元数据字段信息
      # 校验
      validate:
        # 状态码校验
        code: 200
        # 返回值校验
        response: [{'childAssets': [{'assetId': 'system_admin_log_audit','assetName': '审计日志'}],'assetId': '1','assetName': '统一基础平台'}]