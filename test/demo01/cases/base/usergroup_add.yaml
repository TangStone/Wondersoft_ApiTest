- epic: 用户管理   # 一级模块
  feature: 基础数据管理-用户与机构管理  # 二级模块
  story: 新增用户组    # 接口名称
  caseid: usergroup_add_01
  # 用例名称
  name: 新增用户组
  # 用例描述
  description: 新增用户组
  steps:
    - description: 新增用户组
      api: [ "统一平台接口信息.xlsx","用户与机构管理","1" ]   #接口信息文件路径，sheet页，接口id
      data:
        groupName: 自动化测试根组
        parentGroupId: '1'
      # 校验
      validate:
        # 状态码校验
        code: 200
        # 返回值校验
        response: { "statusCode": 0,"msg": "success" }
        # 数据库校验
        dbcheck:
          - type: mysql
            sql: select * from `bms-general-uba`.t_group_info where groupName = '自动化测试根组' and isRemoved = '0'
            result:
              - path: $.parentGroupId
                value: 1
